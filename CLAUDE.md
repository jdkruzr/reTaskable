# reTaskable

Last verified: 2026-06-21

A CalDAV task client for the reMarkable Paper Pro (rMPP), running under the AppLoad on-device harness. Rust backend + QML frontend. Speaks to any RFC-compliant CalDAV server; test targets are Nextcloud (`checkwithscience.com`) and a go-webdav server (UltraBridge) — see M12.

Latest milestone: **M16 — due date / time on create + edit (the last functional gap before 1.0).** Both the Create row and the task-detail dialog now carry a **segmented, tappable/typeable due picker** (`app/ui/DueField.qml`, a new reusable component bundled via `application.qrc`): `YYYY · MM · DD` boxes (each a bordered `TextInput`, tap-to-focus → keyboard) plus a `+ Time` toggle that reveals `HH:MM`, one-tap presets **Today / Tomorrow / +1 wk**, and **Clear**. It emits a normalized **token** crossing the wire so QML stays ignorant of iCalendar param syntax: `""` (none), `"YYYYMMDD"` (all-day), or `"YYYYMMDDTHHMMSS"` (timed). Granularity is **date by default, optional time, floating-local — no `Z`, no `TZID`** (a single-device personal client): all-day renders `DUE;VALUE=DATE:YYYYMMDD`, timed renders `DUE:YYYYMMDDTHHMMSS`. Backend: two pure helpers in `nextcloud.rs` — `due_property_line` (token → the full `DUE` property text, or `None` for empty/malformed, which is *why* it can't reuse `apply_vtodo_mutations`: that engine only emits `KEY:value` and can't write the `;VALUE=DATE` parameter) and `set_due` (a dedicated VTODO scanner that drops any existing `DUE` line — matching both `DUE:` and `DUE;…` — and inserts the fresh one before `END:VTODO`, clearing on an empty token). `due` threads through `db::enqueue_create_with_anchor` (a conditional DUE line in the VTODO `format!` + the token into the cache `due` column, so a freshly-created task shows its due *before* sync) and `db::enqueue_edit` (`Some(token)` sets/clears, `None` preserves the existing DUE; the op payload carries the token so `queue::dispatch_edit`'s 412-retry closure re-applies `set_due` and a conflict can't drop the date). **No new MSG numbers:** `create` (MSG 8) changed from a bare summary string to JSON `{summary, due}` (with a non-JSON fallback = bare summary), and `edit_by_uid` (MSG 19) gained a `due` field. The app also got a `formatDue()` helper rendering the raw `DUE` value as `"Jun 22"` / `"Jun 22 2026"` / `"Jun 22 · 2:00 PM"` in both the row and the detail line. Three tofu/UX fixes rode along, all hardware-found: the completed-checkbox check is now a **filled inner square** (the device font lacks ✓/Dingbats — it rendered as tofu; the filled square is font-independent and crisp on e-ink), the time toggle uses plain ASCII `+ Time`/`- Time` (the fullwidth `＋`/`−` were also tofu), and the Create button now drops focus + `Qt.inputMethod.hide()` so the keyboard doesn't linger (mirroring `closeDetail()`). **No xovi/`.qmd` change — pure app + backend** (138 backend tests; nothing to hash). Hardware-verified on UltraBridge: create with preset / typed date / +time / no-due, edit to reschedule and to clear, offline queue → reconnect flush carries the due. **A note-captured to-do still arrives with no due** — adding a due picker to the xochitl capture dialog is the post-1.0 next milestone (it can't reuse `DueField.qml`: that dialog runs in xochitl's QML process via qmldiff injection, so the picker would be re-implemented inline in the hashed `.qmd`). Prior: **M15 — jump-back navigation: a note-captured to-do's "📓 Open note" closes the app and opens the source notebook at the captured page, closing the note→task→note loop (see the M15 bullet below).** Before that: **M14 Phase 2 — the in-note confirm/edit dialog (completing the "lasso → to-do" capture UX).** Every capture now shows a dialog in xochitl: pre-filled with the recognized text (editable), or — when recognition is unavailable — an empty field with the notice "Handwriting recognition unavailable — type your task". The to-do is written **only on an explicit Create**, so an accidental pen-touch can't lose or mangle a capture, and the note is never altered. The hook (`app/xovi/retaskableSend.qmd`) is now **four** coordinated patches: (A) the lasso-menu button + `sendToReTaskable` signal on `SceneSelectionHandler`; (B) `DeviceSceneView` bubbles a bare `reTaskableCapture()` up, exposes `reTaskableConvertOnline()` (the stock-gated convert-on-clone, run only when online), and relays `reTaskableRecognized(text, ok)`; (C) `HwcSelection` recognizes (`Recognizer.startSelectionToolJob`, ~25 s timeout → `abortSelectionToolJob`) and, in the `REBUILD`'d `handwritingBar.onFinishedChanged`, **emits** `reTaskableRecognized(plainText, !failed)` instead of writing; (D) `DocumentView` holds the **connectivity gate + the dialog**. Both live in DocumentView because it is the only scope with the live `PopupOverlay` (`id _popupOverlay`, inside `Item { id _uiContainer }`), the pen-input manager, **and** `LinkProvider`/`Authentication` in scope. The gate mirrors stock `onConvertSelection` (`!Authentication.loggedIn || LinkProvider.availability === LinkProvider.Offline`): offline / not-signed-in opens the empty type-fallback (avoiding the stock "requires a Wi-Fi connection" toast); online runs `sceneView.reTaskableConvertOnline()`. The dialog is a native `Popup`+`HeaderInput` cloned from xochitl's `AddTagPopup` (`/qml/device/view/documentview/AddTagPopup.qml`), wired `overlay: _popupOverlay`, **deliberate-modal** (`tapOutsideBehavior: Popup.TapOutside.Ignore` + `outsidePenInputBlocked: true` — pen-on-canvas is a no-op); `HeaderInput.onAccepted` → `writeIntake(text)` (the M13 `cap_<ts>.json` PUT + Phase-1.2 `--sync-once` spawn, **moved here from C**; anchor read locally via `root.document.id` / `root.currentPage`), `onRejected` → discard. **Backend unchanged** — a pure hook change: the intake schema, `drain_intake`, queue, and `--sync-once` are identical; the dialog only changes *when* and *with what text* the existing file is written. Hardware-verified: online recognize→edit→Create→sync (a MyScript "This isatest" corrected to "This is a test" by hand); offline type→Create→queued→flush-on-reconnect; the gate (no Wi-Fi toast); Cancel (no task); pen-on-canvas (no ink). Offline dev loop: `qmldiff apply-diffs <root> <out> retaskableSend.qmd -f` validates the readable diff against staged decompiled source (`SceneSelectionHandler`=q_214, `DeviceSceneView`=q_138, `DocumentView`=q_151, `HwcSelection`=q_125) before deploy; ships **hashed** (publish rule unchanged; command-executor identifiers stay readable). **Since shipped (app-side UX pass, hardware-verified):** a **task-detail dialog** — single-tap a row's text (a full-delegate `MouseArea` *below* the `Row`, so plain `Text` taps fall through while the checkbox keeps its own) opens a full-screen overlay showing the row's info un-elided (status, due, `📓` source, sync state, UID) with **edit-summary** (wrapping `TextArea`) and a **two-step delete**; writes go through UID-addressed handlers **MSG 19/119 `edit_by_uid`** (`{uid,summary}`) and **20/120 `delete_by_uid`** (uid), both mirroring `toggle_by_uid`; `closeDetail()` drops focus + `Qt.inputMethod.hide()` so the keyboard never lingers. Plus three legibility fixes: a **fixed three-line status slot** (`maximumLineCount: 3` + fixed height, always reserved) so changing message length never reflows the controls/list; the action buttons collapsed onto **one line** (Sync · Settings · Show/Hide Completed · ▲ · ▼); and the conditional **Resolve-Conflict button relocated beside the status slot** (status auto-narrows when it shows) so it never disturbs the button row. **M15 (shipped, hardware-verified) — the jump-back.** The detail dialog shows a **📓 Open note** button for note-captured to-dos; it sends **MSG 21 `open_note`** (payload `"<docUuid>,<pageKey>"`), and the backend writes `ureTaskableJump:<doc>,<page>` to the xovi message-broker pipe `/run/xovi-mb` — the **`u`** prefix routes to QML (`e` is native-only; the trap that ate a chunk of the spike). A new hook **`app/xovi/retaskableJump.qmd`** (a `XoviMessageBroker` listener in `MainView`, `createQmlObject`'d so a module-load failure can't break MainView's load) receives via **`function onSignalReceived(signal, message)`** + `listeningFor: ["reTaskableJump"]` and navigates with MainView's stock `root.onOpened({documentId, page})` (`Library.entryForId` → `documentView.item.openDocumentOnPage`); the app `endpoint.terminate()`s on the MSG 121 reply so the pipe write lands first. The machine anchor (`X-RETASKABLE-SOURCE-{DOC,PAGE}`) now rides the MSG 4/104 envelope as `doc`/`page` (`nextcloud::extract_source_doc`/`extract_source_page`, `db::source_anchors`). **Note:** `sendSimpleSignal` from QML reaches native extensions only, NOT QML listeners — hence the backend-writes-the-pipe design. Two capture-UX follow-ons rode along: the `📓` label now shows the **notebook title** (`retaskableSend`'s `writeIntake` uses `root.document.visibleName`, "reMarkable note" fallback) instead of a generic string; and the detail dialog gained a top-right **← Return to List**. Full spike record (broker receive contract, the `u`-pipe trap, the **`systemctl restart xochitl` reboots this device + drops xovi → reload via tripletap** gotcha) lives in Jarett's memory. **Next (post-1.0):** a due picker in the xochitl capture dialog — can't reuse the app's `DueField.qml` (cross-process), so it'd be re-implemented inline in the hashed `app/xovi/retaskableSend.qmd`.

Prior milestones:
- **M14 Phase 1 / 1.2 — the capture hook + hands-free auto-sync (write timing now superseded by Phase 2).** The lasso-menu button clones the strokes, runs cloud MyScript recognition, and — originally — wrote the M13 intake JSON directly (non-destructive: clone, never delete). Phase 1.2 spawns `entry --sync-once` via the **command-executor** extension (`net.asivery.CommandExecutor`) so a capture reaches the server with the app closed: `main()` branches on `argv[1] == "--sync-once"` *before* `AppLoad::new()`, under an `flock` single-flight lock; `db::open` arms a 5 s `busy_timeout` and creates the intake dir on startup. **No systemd unit** (Jarett's softbrick constraint; tripletap is the rationed exception). Phase 2 moved the *write* into the dialog's Create, but the recognition wiring, intake spool, and `--sync-once` core are unchanged.
- **M13 — note-anchor intake (reTaskable receiving end of "lasso → to-do").** Builds the *receiving* end: an **intake spool** (`~/.local/share/retaskable/intake/*.json`) the backend drains on app launch (MSG 18/118 `drain_intake`, fired by QML on load) and at the top of every `sync()`. Each file → an **anchored create**: `db::enqueue_create_with_anchor` stamps `X-RETASKABLE-SOURCE-{DOC,PAGE,LABEL}` into the VTODO. The label renders as a `📓 …` line under the to-do (parsed back via `nextcloud::extract_source_label`; surfaced in the MSG 4/104 envelope as a per-task `source` field). The anchor survives the offline-queue flush because the create payload carries the full cached body and the queue PUTs it verbatim (`put_task_create_body`) instead of rebuilding from summary — so cache == what's sent == what the server stores (hardware-verified: all three X-props round-tripped through UltraBridge).
- **M12 — CalDAV servers without WebDAV-Sync.** Some servers (e.g. go-webdav) reject the `sync-collection` REPORT (`400 ... unsupported REPORT root "DAV:" "sync-collection"`). The sync path detects this (`nextcloud::sync_collection_unsupported`) and falls back to an RFC 4791 `calendar-query` REPORT (`calendar_query_all`), enumerating every VTODO each sync (no sync-token → always a full pull, reusing `parse_sync_response`). `db::set_last_synced` records a sync time without a token so token-less servers still show "Last synced …". Makes the "RFC-compliant for any server" posture real, not Nextcloud-only.
- **M11 — in-app Settings.** A settings overlay edits the full sync target (base_url, username, app password, calendar) and doubles as first-run setup. New MSG 15/115 `get_config` (returns a `has_password` bool, never the secret), 16/116 `save_config`, 17/117 `discover_with` (validate creds + list calendars *before* saving). `config::{load_optional, save, merge_password}` (blank password = keep existing). `db::reset_cache` fires only when the target (base_url or calendar) changes; a creds-only change keeps the cache + queued ops.
- **M10 — the real ListView UI.** Replaced the M0–M9c button-Flow + single response-Text scaffolding with a scrollable `ListView`/`ListModel`. MSG 4 / response 104 now carry a JSON envelope (`{last_synced, tasks:[{uid,summary,completed,due,mark}]}` via `format_tasks_json`). A per-row checkbox toggles a *specific* task by UID (MSG 14/114 `toggle_by_uid`, reusing `enqueue_toggle`'s optimistic cache flip; the compact reply updates one row, no full redraw). Completed tasks are hidden by default with a Show Completed toggle (`filter_for_display`; MSG 4 payload `"all"`/`"open"`). e-ink: kinetic scrolling is disabled (`interactive: false`) in favor of ▲/▼ discrete paging — one screenful per tap = one clean refresh.
- **M9c — pending-op visualization (now superseded for display).** `format_tasks_marked` prefixed each Show Tasks row with `! `/`* `/`  `. The live path emits JSON now; `format_tasks_marked` is retained only for its tests (`#[cfg_attr(not(test), allow(dead_code))]`).
- **M9b — conflict-resolution UI.** Resolve First Conflict shows a local-vs-server diff (Keep Mine / Take Theirs / Cancel) when a write double-412s. Toggle + edit only.

## Tech stack

- Rust 2021, single binary `retaskable-backend` (no library target — tests run via `cargo test --bin retaskable-backend`)
- rusqlite 0.32 (bundled SQLite, statically linked); schema is at `schema_version = 2`
- reqwest 0.12 with `rustls-tls-webpki-roots` (no system OpenSSL on device)
- tokio 1 (multi-thread runtime; `rt-multi-thread` feature on)
- Other deps: anyhow, uuid, chrono, icalendar 0.16, roxmltree, url, serde_json
- Dev-deps: `httpmock = "0.7"` (release binary is unaffected)
- Vendored: `vendor/appload-client/` (Rust crate that talks to AppLoad's AF_UNIX SOCK_SEQPACKET frames). **We've modified it (`?Send` on the async trait); treat it as forkable, not third-party.**
- Frontend: QML (Qt 5/6 dual-compatible) at `app/ui/Main.qml`

## Commands

| Action | Command |
|--------|---------|
| Host build (emulator) | `cd app && ./build-pc.sh` |
| Cross-build (rMPP aarch64) | `cd app && ./build-rmpp.sh` (requires `source sdk/environment-setup-cortexa55-remarkable-linux` first) |
| Run tests | `cd app/backend && cargo test --bin retaskable-backend` |
| Deploy to device | `cd app && ./install-device.sh` (default IP `10.11.99.1`, USB-net) |
| Watch device logs | `ssh root@10.11.99.1 'journalctl -u xochitl -f' \| grep retaskable` |
| Inspect device DB | `scp -O root@10.11.99.1:/home/root/.local/share/retaskable/db.sqlite /tmp/` then Python's `sqlite3` module (no CLI on busybox) |

The build scripts handle the cross-compile env (linker, sysroot, `CC_*` per-target vars for cc-rs). The install script kills the running backend, wipes the remote dir, scps fresh, and uses `-O` for legacy SCP protocol.

## Project structure

- `app/backend/src/main.rs` — `AppLoadBackend` impl, MSG dispatch, `sync()` (drains intake first), `compose_sync_response`, write handlers (`toggle_first` + the UID-addressed `toggle_by_uid`/`toggle_by_uid_inner`, `create`, `edit_first` + the UID-addressed `edit_by_uid`/`edit_by_uid_inner`, `delete_first` + the UID-addressed `delete_by_uid`/`delete_by_uid_inner`), the M15 `open_note` (+ pure `jump_command`) that writes the jump signal to `/run/xovi-mb`, `show_tasks` (JSON envelope, open/all filter), the M11 settings handlers (`get_config`, `save_config`/`save_config_inner` + `target_changed`, `discover_with`), and the M13 intake (`drain_intake` + testable `drain_intake_from`, `IntakeItem`/`make_anchor`). M16: `create` now parses JSON `{summary, due}` (non-JSON fallback = bare summary) and routes through `enqueue_create_with_anchor` with the due token; `edit_by_uid`/`edit_by_uid_inner` carry an `Option<&str>` due
- `app/backend/src/db.rs` — SQLite schema + migration, cache CRUD, `enqueue_{create,edit,toggle,delete}` (+ M13 `enqueue_create_with_anchor`, now `(…, anchor, due)`; `enqueue_create` is the `None, None` wrapper; M16 `enqueue_edit` takes `due: Option<&str>` — `Some` sets/clears the cache `due` column + body, `None` preserves; both write a validated token into the `due` column so a freshly-created/edited task shows its due pre-sync), `fetch_next_drainable`, `cascade_uid`, `clear_errored`, `pending_marks`, `source_labels` (M13 uid→label map, mirrors `pending_marks`), `source_anchors` (M15 uid→(doc,page) map for jump-back), `reset_cache` (M11 target-switch wipe), `set_last_synced` (M12 token-less sync time), `intake_dir` (M13 spool path), the `Anchor` struct, in-file tests
- `app/backend/src/queue.rs` — `flush_pending`, `dispatch_op`, `apply_outcome`, `classify_error`, `FlushSummary`, `ExecOutcome`, in-file tests including `#[tokio::test]` + httpmock integration. `dispatch_create` PUTs the create payload's cached `ical` body verbatim (M13: carries anchor X-props + M16 DUE line), falling back to a summary rebuild for pre-M13 ops. M16: `dispatch_edit` reads an optional `due` token from the op payload and re-applies `set_due` inside the 412-retry closure (a present key sets/clears, absent preserves) so a conflict re-derive can't drop the date
- `app/backend/src/nextcloud.rs` — HTTP helpers: discovery, `sync_collection_with_fallback` (sync-collection → `calendar_query_all` when `sync_collection_unsupported`), `put_task_with_retry` (412 auto-retry), `delete_task_with_retry`, `put_task_create_body` (M13: PUT an exact VTODO body), iCalendar mutations (`toggle_completion`, `replace_summary`, M16 `set_due` — a dedicated VTODO scanner since `apply_vtodo_mutations` can't emit the `;VALUE=DATE` param — + the pure `due_property_line` token→`DUE`-line helper, `escape_ical_text` + inverse `unescape_ical_text`), M13 anchor read (`extract_source_label`, unfolds + unescapes; M15 `extract_source_doc`/`extract_source_page` for the machine anchor, no unescape), task rendering (`format_tasks_json` live — emits a per-task `source` field, M15 `doc`/`page` fields + a top-level `conflicts` count from `db::count_resolvable_conflicts`; `format_tasks_marked` test-only; `filter_for_display`)
- `app/backend/src/config.rs` — TOML load + write for `~/.config/retaskable/config.toml`: `load`, `load_optional` (None on missing), `save`/`save_to`, `merge_password`
- `app/ui/Main.qml` — scrollable `ListView` of tasks (pending-mark + checkbox + summary + M13 `📓 source` line; the completed-checkbox mark is a **filled inner square**, not a font glyph — the device font lacks ✓), a fixed three-line status slot with the conditional Resolve-Conflict button beside it, a one-line action row (Sync · Settings · Show/Hide Completed · ▲/▼ discrete paging), Create row (now with a `DueField` and a JSON `{summary, due}` MSG 8 send + keyboard-dismiss on Create), the **task-detail dialog** (`openDetail`/`closeDetail` — single-tap a row → full-screen overlay with edit-summary, the M16 `DueField` editor, two-step delete, MSG 19/20), the M16 `formatDue()` friendly-date helper (row + detail), the M9b Keep/Take/Cancel row, the M11 Settings overlay (URL/username/password + calendar picker), and the M15 jump-back **📓 Open note** button (note-captured rows only → MSG 21 → `terminate()` on reply) and the detail dialog's top-right **← Return to List**, and the M13 launch-time intake drain (MSG 18 → `applyDrainResult` loads the list)
- `app/ui/DueField.qml` — M16 reusable segmented due picker (tappable/typeable `YYYY · MM · DD` + `+ Time` toggle for `HH:MM`, Today/Tomorrow/+1 wk presets, Clear). Exposes a read-only normalized `token` (`""` / `YYYYMMDD` / `YYYYMMDDTHHMMSS`) and `setFromToken(raw)` for edit prefill. Registered in `application.qrc`; instantiated in the Create row and the detail dialog
- `app/xovi/` — xochitl xovi+qmldiff hooks (committed **hashed**; unhash with `qmldiff hash-diffs <device-hashtab> <file> -r` to edit, re-hash before commit). `retaskableSend.qmd` (M14: lasso→to-do capture dialog; `writeIntake` labels with the notebook title) and `retaskableJump.qmd` (M15: `XoviMessageBroker` listener in `MainView` → `root.onOpened` jump-back)
- `vendor/appload-client/` — modified AppLoad client crate (the `?Send` trait change is intentional)
- `docs/design-plans/` — per-milestone design plans (read these before changing architecture)
- `docs/implementation-plans/` — per-milestone implementation plans (read these to understand why specific code shape was chosen)
- `docs/test-plans/` — per-milestone human test plans (manual verification scripts)

## Conventions

**MSG protocol.** Requests are 1–21, responses are 101–121, dense numbering so QML can filter with simple `||` chains. Reserved system types: `0xFFFFFFFE` (new-coordinator), `0xFFFFFFFF` (terminate). Frames are `{type:u32 LE, len:u32 LE}` + UTF-8 payload. Notable payloads: **MSG 4 / 104** carry a JSON task envelope (M10; request payload `"open"`/`"all"` selects the completed filter; M13 added a per-task `source` field = the note-anchor label, `""` when absent; M14 added a top-level `conflicts` count — number of resolvable double-412 ops — that gates the "Resolve Conflict" button in the UI; M15 added per-task `doc`/`page` fields = the machine anchor for jump-back, `""` when absent; the `due` field carries the raw iCalendar DUE value the M16 app `formatDue()` renders); **8 / 108** `create` (M16: request is now JSON `{summary, due}` — `due` is the normalized token `""`/`YYYYMMDD`/`YYYYMMDDTHHMMSS`; a non-JSON payload falls back to a bare summary); **14 / 114** `toggle_by_uid` (request = uid; reply = compact `{uid,completed,mark}`); **15–17 / 115–117** settings (`get_config` / `save_config` / `discover_with`, JSON both ways — `get_config` returns `has_password` but never the secret); **18 / 118** `drain_intake` (M13; request payload ignored; reply = compact `{"ingested":N}`); **19 / 119** `edit_by_uid` (request = JSON `{uid,summary[,due]}` — M16 added the optional `due` token; present key sets/clears DUE, absent preserves) and **20 / 120** `delete_by_uid` (request = uid) — the task-detail dialog's edit/delete, both UID-addressed like 14/114; each replies with a `Queued: …` status string QML treats like 105/108 (status line + list refresh); **21 / 121** `open_note` (M15; request = `"<docUuid>,<pageKey>"`; backend writes the jump command to `/run/xovi-mb`, replies `"ok"` — QML then `terminate()`s); **112** (conflict preview) is multi-line with an `OPID:<n>\n` sentinel QML strips to drive the Keep/Take/Cancel row. Several response bodies are now JSON; QML `JSON.parse` failures are treated as plain status/error text (and clear the task list — see the M11 stale-list fix).

**Async + rusqlite.** `rusqlite::Connection` is `Send` but `!Sync` (RefCell inside), so `&Connection` is `!Send`. The AppLoadBackend trait uses `#[async_trait(?Send)]` (the harness calls it sequentially under a mutex; Send is gratuitous). Async fns inside the backend hold `&mut Connection` not `&Connection`.

**Offline-first writes.** All four write handlers are *synchronous* — they only enqueue. The `Queued: ...` response renders in under a tick. `sync()` is two-phase: `queue::flush_pending(...).await` first, then sync-collection. A mid-drain transient skips sync-collection that pass; terminals continue draining other UIDs. See `docs/design-plans/2026-05-17-m9a-offline-queue.md` for the full model.

**iCalendar on the wire is CRLF.** Always. `nextcloud::ensure_crlf` runs at every parse boundary (XML extraction strips CRs per XML 1.0 §2.11, so calendar-data comes back LF-only and must be restored). ETags include their surrounding quotes — pass through verbatim.

**Tests are in-file.** No separate `tests/` directory. Each module has its own `#[cfg(test)] mod tests { ... }` block using `Connection::open_in_memory()`. Pre-M9a there were 6 tests; M9a brought it to 54; M9b to 86; M9c to 94; M10 to 99; M11 to 107; M12 to 110; M13 to 120; M16 to 138 (the `due_property_line`/`set_due` unit tests + create/edit-with-due round-trips).

**Diagnostics.** `eprintln!("retaskable: ...")` with lowercase fragments and no terminal punctuation. Captured by xochitl → journald. Never log secrets — `save_config`/`discover_with` log only a char count, not the payload.

**e-ink scrolling.** The app has no control over the display refresh (xochitl owns the waveform pipeline; an in-scene AppLoad QML frontend can't set refresh modes — that would require a windowed/qtfb app, a possible future milestone). Kinetic/flick scrolling fights the refresh (one flick spans many slow cycles → lag + ghosting), so lists use `interactive: false` + explicit ▲/▼ paging. Do **not** reintroduce `flickDeceleration` (the `0` value is invalid) or `cacheBuffer: 0` (delegate churn).

**CalDAV sync strategy.** Prefer `sync-collection` (RFC 6578, incremental via sync-token). If the server rejects it (`sync_collection_unsupported`), fall back to a full `calendar-query` (RFC 4791) each sync. sync-collection is *optional* — never assume a server supports it.

**Commits.** Direct-to-main per milestone; no feature branches. Descriptive multi-paragraph commit messages — explain *why*, not just *what*. After hardware verification, push immediately.

## Workflow discipline (small pieces)

Each milestone must verify on real rMPP hardware before the next one is planned. Code review + automated tests are necessary but not sufficient — bugs hide in cross-module contract assumptions that only surface against a real server. Two examples from the offline-queue era:

- **M9a (commit `f3dbd9c`):** `put_task_with_retry` double-mutated the cached body. Survived 54 passing tests and 8 phases of code review; surfaced on the first real toggle round-trip.
- **M9b (commit `5326ee3`):** `ensure_crlf` left a trailing bare CR untouched, so the icalendar crate couldn't parse Nextcloud's GET response. Survived 85 passing tests; surfaced on the first real conflict-preview attempt.

Plan accordingly; budget time for the hardware loop. M9b also added a `manufacture-m9b-conflict.py` test helper at `docs/test-plans/` because natural double-412s require a millisecond-window race that's impractical to trigger by hand — verification needed direct DB-state injection.

## Where to find more context

The forward-looking roadmap and per-milestone history are not in this repo — they live in Jarett's persistent memory at `/home/jtd/.claude/projects/-home-jtd-reTaskable/memory/` (auto-loaded in his sessions). Key files:

- `project_retaskable.md` — architecture overview and roadmap (M0–M12)
- `project_retaskable_status.md` — per-milestone "what shipped" log
- `lessons_learned.md` — gotchas: CRLF, busybox quirks, scp `-O`, cc-rs per-target env, the M9a bug discoveries
- `reference_locations.md` — SDK and external repo paths

For collaborators or AI sessions without access to that memory: the `docs/` tree in this repo is the canonical project-context source. Design plans + implementation plans together describe what every milestone does and why.

## Device specifics

- **Address**: `root@10.11.99.1` over USB-net, passwordless SSH already set up.
- **Userland is busybox** — `ps aux` doesn't work (use plain `ps`), no `sqlite3` CLI (scp the DB locally), `head -N` is `head -n N`, `pkill -f <substring>` matches against argv.
- **Install path**: `/home/root/xovi/exthome/appload/retaskable/`. AppLoad keeps backends alive after the frontend closes; `install-device.sh` always `pkill`s first to avoid re-attaching to a stale process.
- **Screen**: ~954 px wide portrait. Layouts use `Flow { width: parent.width }` to wrap cleanly.
- **DB lives at** `/home/root/.local/share/retaskable/db.sqlite`. First launch after upgrading from a v1 schema drops and recreates tables (intentional — the next Sync repopulates from the server, which is authoritative).

## Boundaries

- Safe to edit: anything under `app/`, `docs/`
- Vendored but forkable: `vendor/appload-client/` — modifications are intentional (see the `?Send` change), but consult the design history before changing further
- Build artefacts (gitignored): `app/backend/target/`, `app/output-rmpp/`, `app/output-pc/`
- User config (not in repo): `~/.config/retaskable/config.toml`
