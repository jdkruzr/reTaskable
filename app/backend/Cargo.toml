[package]
name = "retaskable-backend"
version = "0.1.0"
edition = "2021"

[dependencies]
appload-client = { path = "../../vendor/appload-client" }
async-trait = "0.1"
tokio = { version = "1", features = ["macros", "rt", "rt-multi-thread"] }
